// Arquivo de configuração para o Assistente de Pitch
// Renomeie este arquivo para 'config.js' e adicione sua chave da OpenAI

const CONFIG = {
    // Substitua pela sua chave real da OpenAI
    OPENAI_API_KEY: "sk-sua-chave-aqui",
    
    // Modelo da OpenAI (pode alterar se quiser)
    MODEL: "gpt-4o-mini",
    
    // Configurações de geração
    MAX_TOKENS: 1000,
    TEMPERATURE: 0.7
};

// Para usar no HTML, adicione esta linha antes do script principal:
// <script src="config.js"></script>
